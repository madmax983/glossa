print("Ah! The `git commit --amend` I did earlier overwrote the HEAD commit (`6a60868 🎨 Mosaic: UI Polish for Haruspex (#1244)`) and replaced it with `beb659a` which is STILL the mosaic commit because I used `--amend` and `--no-edit`.")
print("Wait, `--amend` changed the commit of the current branch, which was my PR! I didn't actually create a new commit! I amended my changes onto the existing commit.")
print("So `beb659a` CONTAINS MY CHANGES!!!")
print("Let's check!")
