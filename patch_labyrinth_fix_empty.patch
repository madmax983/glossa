--- src/tools/labyrinth.rs
+++ src/tools/labyrinth.rs
@@ -62,7 +62,7 @@
     let mut table = Table::new();
     table.load_preset(presets::UTF8_FULL);

-    if cfg.trim() == "graph TD" {
+    if program.statements.is_empty() {
         table.set_header(vec![
             Cell::new("Status")
                 .add_attribute(Attribute::Bold)
