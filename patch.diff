--- src/semantic/control_flow.rs
+++ src/semantic/control_flow.rs
@@ -936,7 +936,7 @@
         };

         // Let's create an invalid phrase that fails inside extract_value
-        // By supplying a verb but no arguments it should be an invalid value
+        // By supplying an empty phrase... wait, extract_value handles verbs if they are variables?
         let clause2 = Clause {
             expressions: vec![Expr::Phrase(vec![
                 Expr::Word(Word::new("δός")),
