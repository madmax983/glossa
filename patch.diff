--- src/tools/tester.rs
+++ src/tools/tester.rs
@@ -334,17 +334,11 @@
                     name.cyan().bold().underlined()
                 );
                 // Create a box for the error message
-                let border_top =
-                    "╭───────────────────────────────────────────────────────────────────╮".red();
-                let border_bottom =
-                    "╰───────────────────────────────────────────────────────────────────╯".red();
-
-                println!("{}", border_top);
-                for line in msg.lines() {
-                    // Wrap extremely long lines if needed, but for now simple print
-                    println!("{} {}", "│".red(), line);
-                }
-                println!("{}", border_bottom);
+                let mut err_table = Table::new();
+                err_table.load_preset(presets::UTF8_FULL);
+                let cell = Cell::new(&msg).fg(Color::Red);
+                err_table.add_row(vec![cell]);
+                println!("{err_table}");
                 println!();
             }
         } else {
