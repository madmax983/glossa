**[Replacing unwrap in assembly tests]**
**Learning:** `unwrap()` is prevalent across tests, especially `src/semantic/assembly/mod.rs`. While it serves for assertions, Sentry advocates for replacing test `.unwrap()` calls by updating test functions to return `Result<(), AssemblyError>` and using the `?` operator, which leads to robust, error-propagating tests.
**Action:** Used python script text processing to update test function signatures across `src/semantic/assembly/mod.rs` to safely return `Result`s and substituted `.unwrap()` with `?`. Keep an eye out for similar patterns in other modules like morphology.
