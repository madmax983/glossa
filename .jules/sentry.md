**[Control Flow Error Coverage]**
**Learning:** Found untested error paths (`Err` branches) and conditionals (`for_range`, `for_iteration`, `match`) in `src/semantic/control_flow.rs`. Added tests explicitly triggering the bounds conditions, such as for loops without sufficient clauses, to hit the `Err` returns.
**Action:** Always create tests asserting specific error strings via `unwrap_err().to_string().contains(...)` to ensure correct error paths are covered. Found that destructuring `AnalyzedStatement` types in integration tests with `_ => panic!(...)` introduced false uncovered branches; converting to `let else` resolved this issue.
